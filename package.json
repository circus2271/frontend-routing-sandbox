{
  "name": "routing-player",
  "version": "0.0.1",
  "scripts": {
    "dev": "rimraf ./dist && parcel watch src/index.html --no-hmr & live-server --port=8000 ./dist/ --entry-file=index.html",
    "build": "rimraf ./dist && parcel build src/index.html"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead, iOS 13",
  "devDependencies": {
    "@parcel/resolver-glob": "^2.9.3",
    "@parcel/transformer-sass": "^2.9.3",
    "@parcel/validator-typescript": "^2.9.3",
    "live-server": "^1.2.2",
    "parcel": "^2.9.3",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
